# =================================================================
# Gan Shmuel Environment Configuration Template
# =================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all 'changeme' values with secure passwords
# 3. NEVER commit the .env file to version control
#
# SECURITY NOTES:
# - Use strong passwords (min 16 characters, mixed case, numbers, symbols)
# - Rotate passwords regularly
# - Keep .env in .gitignore
# - For production, use a secrets manager (HashiCorp Vault, AWS Secrets Manager)
#
# =================================================================

# -----------------------------------------------------------------
# MySQL Root Password (shared across all MySQL databases)
# -----------------------------------------------------------------
MYSQL_ROOT_PASSWORD=changeme_use_strong_password

# -----------------------------------------------------------------
# Weight Service Database (Port 3306)
# -----------------------------------------------------------------
WEIGHT_DB_NAME=weight_db
WEIGHT_DB_USER=weight_user
WEIGHT_DB_PASSWORD=changeme_weight_password

# -----------------------------------------------------------------
# Billing Service Database (Port 3307)
# -----------------------------------------------------------------
BILLING_DB_NAME=billdb
BILLING_DB_USER=bill
BILLING_DB_PASSWORD=changeme_billing_password

# -----------------------------------------------------------------
# Shift Service Database (Port 3308)
# -----------------------------------------------------------------
SHIFT_DB_NAME=shift_db
SHIFT_DB_USER=shift_user
SHIFT_DB_PASSWORD=changeme_shift_password

# -----------------------------------------------------------------
# Provider Registration Service Database (PostgreSQL, Port 5432)
# -----------------------------------------------------------------
PROVIDER_DB_NAME=provider_registration
PROVIDER_DB_USER=provider_user
PROVIDER_DB_PASSWORD=changeme_provider_password

# -----------------------------------------------------------------
# Monitoring (Grafana - Port 3001)
# -----------------------------------------------------------------
# Default: admin/admin (CHANGE THIS IN PRODUCTION!)
GRAFANA_ADMIN_PASSWORD=changeme_grafana_password

# Grafana API Key for programmatic access
# Generate at: http://localhost:3001/org/apikeys
# Example: glsa_XXXXXXXXXXXXXXXXXXXXXX_xxxxxxxx
GRAFANA_API_KEY=changeme_grafana_api_key

# -----------------------------------------------------------------
# Service URLs (Internal Docker Network)
# -----------------------------------------------------------------
# These are used for service-to-service communication
# DO NOT change unless you modify docker-compose service names
WEIGHT_SERVICE_URL=http://weight-service:5001
BILLING_SERVICE_URL=http://billing-service:5002
SHIFT_SERVICE_URL=http://shift-service:5003

# -----------------------------------------------------------------
# Redis (Port 6379)
# -----------------------------------------------------------------
# Used by Shift Service for caching
REDIS_URL=redis://shift-redis:6379/0

# -----------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------
# Directory for file uploads (container path)
UPLOAD_DIRECTORY=/in

# -----------------------------------------------------------------
# JWT Configuration (Provider Registration Service)
# -----------------------------------------------------------------
# Generate a secure secret: openssl rand -hex 32
# JWT_SECRET_KEY=changeme_jwt_secret_minimum_32_characters

# -----------------------------------------------------------------
# Optional: External Monitoring
# -----------------------------------------------------------------
# Uncomment if using external Prometheus/Grafana
# PROMETHEUS_URL=http://prometheus:9090
# GRAFANA_URL=http://grafana:3001

# -----------------------------------------------------------------
# Optional: Log Levels
# -----------------------------------------------------------------
# Uncomment to override default log levels
# LOG_LEVEL=INFO
# WEIGHT_SERVICE_LOG_LEVEL=DEBUG
# BILLING_SERVICE_LOG_LEVEL=INFO
# SHIFT_SERVICE_LOG_LEVEL=INFO
# PROVIDER_SERVICE_LOG_LEVEL=INFO

# -----------------------------------------------------------------
# Optional: Production Overrides
# -----------------------------------------------------------------
# Uncomment for production deployment
# ENVIRONMENT=production
# DEBUG=false
# ENABLE_CORS=false
# ALLOWED_ORIGINS=https://yourdomain.com

# =================================================================
# END OF CONFIGURATION
# =================================================================
